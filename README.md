# File Automator 📂

A simple Python automation script that organizes files in a directory based on their file extensions.

## How it works
1. Scans the current folder.
2. Detects file types (Images, Documents, Audio, etc.).
3. Moves them into appropriate sub-folders automatically.

## How to use
1. Run the script:
   ```bash
   python cleaner.py